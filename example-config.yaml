# Example photobook configuration

# List of directories containing photos - photos from every listed folder
# are merged into one pool before layout.order is applied
# Paths can be relative or absolute
#photo_folders:
#  - /mnt/pictures/2026/2026-04-30+ Hamburg
photo_folders:
  - /mnt/pictures/2026/Test

# Output configuration
output:
  # Paper size: A4, Letter, or custom dimensions
  size: A4

  # Output format: pdf, png, jpg, or html
  format: pdf

  # Output filename (optional - defaults to config filename)
  filename: /mnt/downloads/my-photobook.pdf

  # Output directory (optional - defaults to current directory)
  # directory: ./output/

  # --- format: html only ---
  # Generates a single self-contained HTML file that plays the photobook as
  # an endless, autoplaying slideshow (one slide per photo/title). It's
  # always written into the first photo_folders entry - output.directory and
  # output.filename's directory are ignored for this format, since that's
  # what keeps the slideshow's relative photo paths correct; only a bare
  # filename override is honored. See README.md's "HTML Slideshow Output"
  # section for details.
  # format: html
  # interval_seconds: 5  # seconds per slide (optional, default 5)

# Layout configuration
# Specify EITHER photos_per_page OR pages (not both)
layout:
  # Fixed number of photos per page
  photos_per_page: 3
  
  # OR: Fixed total number of pages
  # pages: 8
  
  # Photo ordering (optional)
  # order: alphabetical  # alphabetical (default) or date

  # Start a new page whenever a photo/title is from a new calendar day
  # (optional, default true). With a fixed `pages` count, set to false to
  # go back to filling pages purely by photo count, ignoring dates.
  # new_page_per_day: true

# Theme selection
# Available: clean, classic, modern
# Or path to custom theme file
theme: clean2

text_labels:
  # 'title' entries get their own page slot (like a photo) instead of being
  # overlaid on the nearest photo - use them for section/chapter headings.
  - timestamp: "2026-04-30T10:00:00"
    title: |
      # **Hamburg**

      30. April 2026
  - timestamp: "2026-04-30T12:23:00"
    text: "## Ladepause"
  - timestamp: "2026-04-30T14:02:00"
    text: "Es geht los"
  - timestamp: "2026-04-30T14:04:00"
    text: "**Hamburger** Spendenparlament"
