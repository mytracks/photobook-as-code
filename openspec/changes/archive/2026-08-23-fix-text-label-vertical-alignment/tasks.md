## 1. Core rendering fix

- [x] 1.1 In `_wrap_markdown_lines`, track each line's `line_top = min(bbox[1] over words in line)` and `line_bottom = max(bbox[3] over words in line)`, and compute that line's height as `line_bottom - line_top` instead of `max(word_height)`; verify against a hand-computed mixed cap/x-height/descender example (e.g. "Ein neuer Morgen" at a known font/size) that the new height matches `line_bottom - line_top`.
- [x] 1.2 Thread each line's `line_top` through `all_lines_info` (alongside the existing `word_infos`, `line_height`, `line_width`) so `_draw_wrapped_lines` receives it per line.
- [x] 1.3 In `_draw_wrapped_lines`, draw each line's words at `current_y - line_top` instead of raw `current_y`, so the line's actual topmost ink lands at the intended position while all words in the line keep the same shared baseline (verify: rendering "Ein neuer Morgen" per design.md's worked example produces top and bottom margins of exactly the configured padding).

## 2. Test suite alignment

- [x] 2.1 Review `tests/test_renderer.py`'s wrap/blank-line tests (`_measure`, `_render_wrap_box`, `_box_height_at`, `_has_ink`, etc.) against the new line-union-based height and shifted draw position; recompute/update any expected pixel bands that assumed the old per-word-max height, and verify the full existing suite passes. (The wrap/blank-line tests themselves needed no changes - their loose "ink present somewhere in this band" assertions already tolerated the fix. The x/dock-position tests in the same file did: they sampled a fixed row a few pixels below the box top expecting pure background, which the fix's now-tighter top margin turned into glyph ink; moved the sampling row deeper into the box, away from the single line of text. Full suite: 217 passed.)
- [x] 2.2 Add a regression test asserting an auto-height text box's top and bottom margins each equal the configured `text_padding` when its content has no descenders, per specs/text-labels "Auto-sized text box padding is respected regardless of line content". (`test_text_label_auto_height_box_padding_symmetric_without_descenders`, using "Wall over" - mixed cap/x-height tops, shared baseline.)
- [x] 2.3 Add a regression test asserting the bottom margin still equals the configured padding when the box's content does include a descender (e.g. a word ending in "g"), covering the same requirement's second scenario. (`test_text_label_auto_height_box_padding_symmetric_with_descender_line`, using "Wall over\ngap".)
- [x] 2.4 Add a regression test asserting consistent inter-line spacing between a line containing a descender and an adjacent line containing only letters with no ascenders/descenders, per specs/text-labels "Consistent line-to-line spacing regardless of content". (`test_text_label_inter_line_gap_consistent_regardless_of_descenders`, comparing "gap\nWall" vs "Ants\nWall".)
- [x] 2.5 Add a regression test asserting that words sharing a display line render on a common baseline regardless of letterform mix, per specs/text-labels "Words on a rendered line share a common baseline" - this guards against a future fix regressing to per-word top-alignment (design.md's rejected alternative). (`test_text_label_words_on_line_share_baseline`, comparing "Wall"'s and "over"'s bottom ink row.)

## 3. Manual verification

- [x] 3.1 Render the real "Ein neuer Morgen" caption from `hamburg.yaml` (via `render_text_label` directly or a generated sample PDF) and visually confirm symmetric top/bottom margins in its background box. (Confirmed: 12px top vs 13px bottom margin, using the `clean` theme's actual DejaVuSansMono/42/padding-12 settings - previously ~19px vs ~4px.)
- [x] 3.2 Render a multi-line title through `render_title_slot` and visually confirm its vertical centering no longer sits low in its slot. (Confirmed with a two-line title ("Ein neuer Morgen\nin Hamburg", DejaVuSansMono/64/padding-16, centered): 106px top slack vs 108px bottom slack - visually centered.)
